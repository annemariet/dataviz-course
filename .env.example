# Copy to `.env` at the repo root (never commit `.env`).

# Required for notebooks/06_quiz_scores.py and notebooks/07_chart_scoring.py
TEACHER_EMAIL=you@example.edu

# Optional — paths relative to repo root unless absolute
# NOTES_DIR=notes
# PROJECT_EVAL_DIR=project_eval
# CHART_SCORING_DIR=chart-scoring
# EVAL_CSV=Evaluation Sheet - Verified(Sheet1).csv
# TEAMS_CSV=Enregistrement pour le mini-projet(Teams).csv
# CHART_SCORES_FILE=Score this chart.xlsx
